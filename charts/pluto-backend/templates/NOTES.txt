{{- /* NOTES.txt */ -}}
{{- if .Values.ingress.enabled }}
Your application can be reached via Ingress at:

  http://<your-ingress-host>/
{{- else }}
Your application is exposed via the Envoy Service:
  Service: {{ include "pluto-backend.fullname" . }}-envoy
  Port:    {{ .Values.envoy.port }}
  To port-forward locally:
    kubectl port-forward svc/{{ include "pluto-backend.fullname" . }}-envoy {{ .Values.envoy.port }}:{{ .Values.envoy.port }} -n {{ .Release.Namespace }}
{{- end }}

Manifest API:
  http://localhost:{{ .Values.envoy.port }}/api/

Swagger UI:
  http://localhost:{{ .Values.envoy.port }}/
